// import { useDispatch} from "react-redux"
// import { SetUser } from "../redux/actions/useracrions"

const Header = ()=>{
//    let dispatch = useDispatch()
//    dispatch(SetUser('giil'))

    return(
        <h1>hedare </h1>
    )
}

export default Header